apiVersion: v1
kind: Service
metadata:
  name: ai-service
  labels:
    app: onboard-ai-service
    tier: ai
spec:
  type: ClusterIP
  selector:
    app: onboard-ai-service
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
