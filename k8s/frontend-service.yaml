apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: onboard-frontend
    tier: frontend
spec:
  type: NodePort
  selector:
    app: onboard-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
