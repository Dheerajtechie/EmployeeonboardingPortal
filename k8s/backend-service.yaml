apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: onboard-backend
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: onboard-backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
